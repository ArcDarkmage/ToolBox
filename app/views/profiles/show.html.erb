
<div class="container mt-5 d-flex flex-row justify-content-around">
    <div>
      <h3 class = "text-center" >Minhas ferramentas:</h3>
      <ul>
        <% @user.tools.each do |tool|%>
          <li class= "list-group-item">
            <%= link_to tool.name, tool_path(tool), class: "text-decoration-none text-reset" %>
            <!--<%= link_to edit_tool_path(tool) do %>
              <i class="fa-regular fa-pen-to-square"></i>
            <% end %>
            <%= link_to root_path do %>
              <i class="fa-regular fa-trash-can"></i>
            <% end %>-->
          </li>
        <%end%>
        </ul>
      <p><%= link_to "Cadastrar uma nova ferramenta", new_tool_path %></p>
    </div>

  <div>
    <h3 class = "text-center" >Meus aluguéis:</h3>
    <ul>
    <% @user.rents.each do |rent|%>
      <% next if rent.date_end < Date.today %>
      <li class= "list-group-item" >
        <%= "#{rent.tool.name} #{rent.date_start.strftime("(de: %d/%m/%Y")} #{rent.date_end.strftime(" até: %d/%m/%Y)")}" %>
      </li>
    <%end%>
    </ul>
  </div>
</div>
