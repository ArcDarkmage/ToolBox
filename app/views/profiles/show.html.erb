
<div class="container mt-5 d-flex flex-row justify-content-around">
  <%if @user.tools.size >= 1 %>
    <div>
      <h3>Minhas ferramentas:</h3>
      <ul>
        <% @user.tools.each do |tool|%>
          <li>
            <!--Incluir link para detalhes da ferramenta-->
            <%= tool.name %>
            <!--Alterar root_path para tool_path-->
            <%= link_to root_path do %>
              <i class="fa-regular fa-trash-can"></i>
            <% end %>
            <!--Alterar root_path para tool_path-->
            <%= link_to root_path do %>
              <i class="fa-regular fa-pen-to-square"></i>
            <% end %>
          </li>
        <%end%>
      </ul>
    </div>
  <%end%>
  <div>
    <h3>Meus aluguéis:</h3>
    <ul>
    <% @user.rents.each do |rent|%>
      <% next if rent.date_end < Date.today %>
      <li><%= "#{rent.tool.name} #{rent.date_start.strftime("(de: %d/%m/%Y")} #{rent.date_end.strftime(" até: %d/%m/%Y)")}" %></li>
    <%end%>
    </ul>
  </div>
</div>
